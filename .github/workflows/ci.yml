name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v2
        with:
          install: true
      - run: CI=true mise x -- pnpm install --frozen-lockfile
      - run: mise x -- pnpm run type-check

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v2
        with:
          install: true
      - run: CI=true mise x -- pnpm install --frozen-lockfile
      - run: mise x -- pnpm exec oxlint --format github
      - run: mise x -- pnpm exec biome ci --reporter github

  format:
    name: Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v2
        with:
          install: true
      - run: CI=true mise x -- pnpm install --frozen-lockfile
      - run: mise x -- pnpm run format:check

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v2
        with:
          install: true
      - run: CI=true mise x -- pnpm install --frozen-lockfile
      - run: mise x -- pnpm run test
